// vector<vector<Point>> approx_contours(contours.size());
// for (int contour_number = 0;
//      (contour_number < contours.size()); contour_number++)
//   approxPolyDP(Mat(contours[contour_number]),
//                approx_cont ours[contour_number], 3, true);